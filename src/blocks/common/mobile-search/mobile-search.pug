//-
    name: mobile-search
    description: Markup for the search form in mobile main menu

mixin mobile-search()
    form(action="#" method="post").mobile-search&attributes(attributes)
        input(type="text" name="s" placeholder="Search...").mobile-search__input
        button(type="submit").mobile-search__submit
            +icon('search', 'Search')
