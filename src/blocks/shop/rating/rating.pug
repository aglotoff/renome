mixin rating(value)
    .rating&attributes(attributes)
        span.rating__value Rated: #{value} out of 5

        span.rating__stars
            span.rating__highlighted(style={
                width: (value / 5 * 100) + '%',
            })
