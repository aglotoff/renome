mixin rating(value)
    .rating&attributes(attributes)
        span.rating__stars
            span.rating__highlighted(style={
                width: (value / 5 * 100) + '%',
            })

        span.rating__value= value
