extends ../layouts/base.pug

block page
    +page-header({
        title    : data.pageCategory ? data.pageCategory : data.pageTitle,
        subtitle : data.pageSubtitle,
        bgImage  : data.pageHeaderBg,
    })
